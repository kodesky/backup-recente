import React from 'react';

import Default from '../../layouts/Default/index';
import { Wrapper } from '../../components';

const Home: React.FC = () => {
  return (
    <Default>
      <Wrapper>
        <h1>Hello World</h1>
      </Wrapper>
    </Default>
  );
};

export default Home;
